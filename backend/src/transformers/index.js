const transformCourse = require('./transformCourse');
const transformSimulation = require('./transformSimulation');
const transformError = require('./transformError');

module.exports = {
  transformCourse,
  transformSimulation,
  transformError,
};
